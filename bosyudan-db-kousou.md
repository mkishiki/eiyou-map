# 食べあわせ図鑑 ｜ 母集団DB構想メモ

> ゆくゆくの開発用。今すぐの実装ではなく、大分類シルエット等が一段落した後に着手する構想。

## 一行でいうと

いまは任意の144件を手で持っているが、将来は**日本食品標準成分表（公式・膨大）を母集団DBに据え、そこから「初期メン（レギュラー）」を呼び出して表示する**二段構えにする。

---

## なぜやるか（利点）

- **公式の巨大データが既に構造化されて存在する**のが食べあわせ図鑑の最大の強み。DBの練習台としても本番の土台としても最適。
- 食材追加が「栄養量を調べて手入力」から「**母集団から呼び出してフラグを立てるだけ**」になる。含有量・出典が自動でついてくる。
- 母集団を持てば表示の144件に**天井がなくなる**。「なんとなくの塊」から「マニアックな一品」まで連続的につながる。
- 練習の成果物（成分表SQLite・パイプライン）がそのまま本番に使え、チーズハント等にも横展開でき、Data Translatorのポートフォリオにもなる。

---

## データの正体

日本食品標準成分表（文科省がCSV/Excelで公開）
- 食品：約2,500品目
- 栄養素の列：50以上
- 実質「食品 × 栄養素 → 含有量」の関係データ（＝図鑑の「含有」エッジそのもの）

---

## 設計の骨子

### 母集団と初期メンを分ける
- **母集団**：成分表フル（2,500件）をSQLiteに格納。全員がベンチに控えている状態。
- **初期メンのフラグ**：各食材に `is_regular`（レギュラー表示するか）を1列持たせる。
- **表示**：`is_regular = true` の食材だけを図鑑に出す（＝今の144件）。

### 食材追加の流れ（例：ズッキーニ）
1. 母集団を検索してズッキーニを見つける
2. `is_regular` フラグを立てる
3. 再ビルド → 追加完了。栄養量・出典は母集団から自動でついてくる。

### テーブル構成（正規化する場合）
- 食品マスタ（2,500品目）
- 栄養素マスタ（50+）
- 含有量テーブル（食品×栄養素の交差、十数万行）＝本体
- 表示用の144件は、上記への **ビュー／フィルタ** として持つ

---

## 表示アプリは軽いまま（重要）

**表示本体（iPhone単一HTML）はDBを直接持たない**方針を維持する。
DBはあくまで**手元のデータ整備・母集団管理の道具**。そこから「表示用の144件JSON」を書き出す。

**DB（母集団）→ ビルド → 表示用データ** のパイプライン
※ すでにやっている「build scriptでデータ分離」の発想の延長。ai2htmlの「一次データ（版）→ 出力（刷り）」と同じ構造。

### 二つのやり方
- **① 開発時に選ぶ（当面こちら／堅実）**：母集団から `is_regular` で144件を SELECT → 表示JSONを書き出し。追加時はフラグ立てて再ビルド。単一HTMLの軽さを保てる。
- **② アプリ内で選ぶ（発展）**：図鑑に「＋食材を追加」ボタン→その場で母集団検索→合流。ただしアプリが母集団（膨大）を持つ必要が出るため、単一HTMLの軽さと相性が悪い。共有機能や「マイ図鑑」を作るときの姿。母集団はサーバー側に置き、検索だけAPIで叩く形になる。

→ 今の美学（軽い・単一HTML・壊れにくい）だと当面①。

---

## 大分類シルエットとの関係（スケール設計）

現在の「144件を大分類シルエットで見せる」は、巨大データ可視化の王道設計を先取りしている。
- **概観（overview）**：大分類シルエットで「なんとなく認識」（プレアテンティブ処理）
- **詳細（detail）**：掘ると個別・特殊な食材に到達
- 情報探索マントラ「Overview first, zoom and filter, then details-on-demand」そのもの。

母集団を2,500件に広げても、この設計なら破綻せず、むしろ真価が出る：
大分類の塊 → 中分類（魚→青魚…）→ 特殊食材、と天井なく降りられる。
**階層（ツリー：大分類→中分類）× グラフ（末端食材から助ける/妨げるで横断）** の合わせ技になる。

---

## あすけんとの違い（自分の武器の確認）

- あすけん：目的は「記録・管理」。巨大食品DBは食事入力の引き出し。関係性は主役でない。
- 食べあわせ図鑑：目的は「関係を辿って発見」。栄養素同士の助ける/妨げるという**エッジが主役**。
- → 規模で張り合わず、武器は「膨大さ」より**関係の構造化**。母集団は成分表フルで膨らませつつ、主役はエッジに置く。

---

## 着手するときの最小ステップ

1. 成分表CSVをSQLiteに流し込む（まず「入った！」を体験）
2. `SELECT` で遊ぶ（例：たんぱく質が多い順トップ10）
3. 今の144件に `is_regular` フラグを立てる（母集団と初期メンの分離）
4. フラグの立った食材だけ書き出し、今の表示JSONと同じ形にできるか（DB→ビルド）
5. ズッキーニのフラグを立てて再ビルド → 一瞬で追加できるのを確認

※ データ入手先（成分表CSV）の確認から始められる。

---

## 前提・保留メモ

- **先に大分類のシルエット形づくりを終わらせる**のが優先。DB化はその後。
- 入れたい食材の例：ズッキーニ
