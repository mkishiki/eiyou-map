# eiyou map 引き継ぎメモ（v0.727）

最終更新: 2026-08-05

---

## 0. このセッションでやったこと

### アプリ名を変更
- 旧: 食べあわせ図鑑 / eiyou-zukan
- 新: **eiyou map**
- 変更箇所: `<title>`、`apple-mobile-web-app-title`（iPhoneホーム画面のアイコン名）、build.py の出力ファイル名
- 出力ファイル名: `eiyou-zukan.html` → **`eiyou-map.html`**
- ※ データファイル名 `tabeawase-data.js` は未変更（build.py の参照が絡むため、変えるなら別途）

### 権利表記を挿入
- `app-template.html` と `tabeawase-data.js` の**先頭コメント**に以下を挿入（画面には表示しない方針）

```
eiyou map
無断使用・無断転載を禁じます。
All rights reserved. Unauthorized use or reproduction is prohibited.
```

### 食材を14品追加（v0.725〜v0.727）
| 食材 | 一食 | リンク | カテゴリ |
|---|---|---|---|
| ズッキーニ | 80g | 3 | その他の野菜 |
| 鶏レバー | 50g | 15 | 肉・卵 |
| あじ | 80g | 10 | 魚介 |
| 春菊 | 60g | 8 | 緑黄色野菜 |
| オクラ | 60g | 6 | 緑黄色野菜 |
| ちくわ | 50g | 6 | 調味・加工 |
| チンゲンサイ | 80g | 5 | 緑黄色野菜 |
| にら | 50g | 4 | 緑黄色野菜 |
| れんこん | 60g | 3 | その他の野菜 |
| なめこ | 40g | 3 | きのこ |
| かぶ | 60g | 2 | その他の野菜 |
| エリンギ | 40g | 2 | きのこ |
| 桃 | 100g | 1 | 果物 |
| にんにく | 10g | 1 | その他の野菜 |

- 食材数: 133 → **147**
- リンク数: 827 → **896**
- 検証: 整合896/896・閾値もれ0・孤立0・node --check OK

### 見送った候補
- **長ねぎ(30g)・しょうが(10g)**: 10%以上のリンクが0本。薬味で量が少ないため、救済ルールで載せても中心から最も遠い位置に孤立するだけ。「距離が量を表す」という原則が薄まると判断して見送り。

### 個人情報スキャン
- 全ファイルを氏名・メール・電話・さん付け等でスキャン → **ヒット0件**
- 検出された16件は全て公的機関の出典URL（文科省・厚労省等）。公開して問題ない状態。

---

## 1. 次にやること

### (A) GitHub に上げる ← 最優先
**決定事項:**
- **公開リポジトリ**（無料プランだと GitHub Pages は公開リポジトリのみ）
- アカウント名はそのままでOK（本人確認済み）
- 目的: ブラウザで編集したい、会社のWindowsからもアクセスしたい

**手順（未着手）:**
1. リポジトリ作成（名前案: `eiyou-map`）
2. ファイルをドラッグ&ドロップでアップ
3. **GitHub Actions の設定ファイルを追加** ← 中身は次セッションで作成
   - csv をブラウザで編集 → 自動で gen_pct → build → check_data が走る
   - check_data が NG なら赤バツで知らせる（既存の検算がそのまま番人になる）
4. テストコミットして自動ビルドを確認
5. GitHub Pages を有効化 → 公開URL取得（Siri連携の前提にもなる）

**将来的な選択肢として保留:**
- Codespaces（ブラウザ上のLinux環境。重い作業向け。無料枠 月60時間）
- Mac ローカル環境（Homebrew・python3・node は導入済み。作るなら手順は別途）

### (B) 「出典とご注意」の説明を最新・正確にする + ノードのイラストを加える

**現状の構造**（`tabeawase-data.js` 末尾）:
- `SOURCES` … 出典6件（成分表・食品成分DB・食事摂取基準・報告書・HFNet・eJIM）
- `LEVELSRC` … 遠近の算出方法の説明文
- `DISCLAIMER` … 免責文

**修正が必要と判明している箇所:**

1. **LEVELSRC の「点線」記述が実態と合っていない**
   - 現在の文: 「輪郭が点線の食材は含有量が成分表と未照合」
   - 実態: **点線は0件**（gen_pct の出力で「点線（含有量 未確認）: 0件」）
   - → 存在しない仕様を説明している状態。削除または書き換えが必要

2. **DISCLAIMER に「この図鑑は」が残っている**
   - アプリ名が eiyou map になったので表記を見直す

3. **成分表の版の記述**
   - 実際に使っているのは `20260327mxt_kagseimext000029402_02.xlsx`（**2026年3月の正誤版**）
   - SOURCES の記述は「増補2023年」のみ。正誤版を使っている旨を追記するか検討

4. **一食の目安量が暫定・出典なし**という記述は**現在も有効**（そのまま残す）

**ノードのイラストを加える:**
- 目的: 説明パネル内で「大きい丸＝栄養素」「小さい丸＝食材」「近い＝多い」等を図で示す
- 未着手。どういう図にするかの設計から

---

## 2. 積み残しタスク（優先度順）

1. **大分類ノード→タップで展開**（団子の根本解決・大タスク）
2. **Siri連携／食材のID化** … `openFromHash()` は実装済み。GitHub Pages 公開が前提
3. **食材シルエットSVG** … 発注仕様書.md、order-now.csv（93品）、food-id-map.csv（136品）は作成済み。※食材が147品に増えたので**ID mapの更新が必要**
4. 残り約25品の未確認食材のレビュー

---

## 3. ビルド手順

```
src/app-template.html + tabeawase-data.js
        ↓ gen_pct.py（amounts.csv から PCT を自動生成）
        ↓ build.py
    eiyou-map.html
        ↓ check_data.py（検算：整合・閾値もれ・孤立）
```

**注意:**
- `gen_pct.py` は `src/app-template.html` を見る
- `check_data.py` と `build.py` は**直下**の `tabeawase-data.js` を見る
- → **`tabeawase-data.js` は直下と `src/` の両方に必要**

---

## 4. 実行環境の落とし穴（重要）

このセッションで実際に踏んだもの。次回も同じ環境なら再発する。

### create_file が長いと途中で切れる
- 症状: ファイル作成成功と出るのに、直後の実行で「No such file or directory」
- 対策: **スクリプトは短く分ける**。作成直後に `ls -la` で存在確認してから実行

### bash の結果が返ってこない
- 症状: コマンドを投げても応答が来ない
- 起きやすい条件: パイプ（`|`）、複数コマンドの連結、heredoc（`<< EOF`）、for ループ
- 対策: **1コマンド・パイプなし**を徹底。同じコマンドを投げ直せば通ることが多い

### データ構造の罠（実際に2回構文を壊した）
1. **FOODS 配列の終端は `]];`**（`];` ではない）
   - `];` だけを探すと終端検出に失敗し、配列の外に行を挿入して構文エラーになる
2. **`"その他の野菜":[...]` は CATLIST 以外に CATPOS にも存在する**
   - 単純な正規表現だと CATPOS（座標データ）にマッチして、座標配列に食材名を突っ込んでしまう
   - → **`const CATLIST={...]};` のブロック内に限定**してから置換すること
   - CATPOS の終端は `]};`（`\n};` ではない）
- 対策: 適用後に **CATPOS 健全性チェック**（`"名前":[数値,数値]` の2要素のみか）を必ず走らせる

### 成分表 Excel の列マッピング
- **列番号のハードコードは使えない**（メモにあった列番号は現在のファイルと合わず、全栄養素がズレて異常値になった）
- 正解: **12行目の「成分識別子」行から動的に列を引き当てる**
- データは**13行目から**、食品名は**列3**（0始まり）、食品番号は**列1**
- ビタミンKの識別子は `VITK`（**末尾に空白あり**）→ `.strip()` 必須
- ナイアシンは `NE`、塩分は `NACL_EQ`

---

## 5. データのルール

- **リンク閾値**: 一食あたりが一日の目安量の**10%以上**
- **救済ルール**: 全部10%未満なら、最も高い1本だけ残して孤立を防ぐ
- **レベル**: lv1 = 30%以上、lv2 = 15%以上、lv3 = 10%以上
- **基準値**: 日本人の食事摂取基準2025年版、30〜49歳女性（月経あり・身体活動レベルふつう）
- **食材選定**: よく買う食材のみ。成分表の全収載（野菜類だけで数百品）は載せない
- **素材を載せる。完成された料理・飲料は載せない**（コーラは例外として既収載）
- **成分表は生・乾の基本形を使う**（ゆで・油いため等の調理形は使わない）
- 距離は量を表す。栄養素ごとに正規化し、栄養素をまたいだ絶対距離の比較はしない
- 価値判断はしない（塩分も他の栄養素と同じ扱い）

---

## 6. 進め方（このユーザーの好み）

- タメ口。前置き不要
- 実装前に方針を見せる。判断が要る箇所は選択肢を出す。一度に一点ずつ
- 確認していないことを確定形で書かない
- 配布HTMLを直接編集しない。src/ を直して build する
- 低リスクで元に戻せる作業は、逐一確認せず進めてよい（事後報告でよい）
- iPhone が最終確認環境
- 引き継ぎメモは部分修正ではなく**全文差し替え**
- バージョン番号: 第3位を書き出しごとに +1、新機能で第1位 +1
