# 食べあわせ図鑑 ／ データ構造の地図 v1

引き継ぎメモ（`hikitsugi-v0_6.md`）の別紙。**どのデータがどこにあって、何を触ると何に波及するか**をまとめたもの。
「食材を1つ足す／消す」のときに複数の場所を取りこぼさないための地図。

このアプリのデータは正規化されたDBではなく、**1つのJSファイル（`tabeawase-data.js`）の中に複数のテーブル（配列・オブジェクト）が同居**している。
粘菌ネットワークの「線」は、そのうち4つの中間表から来ている。

---

## 全体像（ER的な整理）

### 主役（エンティティ）と件数

単独で存在するもの＝**主役**（鉄・豚もも・骨をつくる 等）。ER図では「エンティティ」と呼ぶ。
このうち画面に点で描かれるのは栄養素と食材（＝ノード）。はたらき・カテゴリは主役だが点にはならない。

| 名前 | 実体 | 件数 | 置き場所 |
|---|---|---|---|
| 栄養素 | `NUT` / 座標 `NPOS` | 26 | data.js（手書き） |
| 食材 | `FOODS` の食材名 / `YOMI` | 144 | data.js（手書き・**csvと揃える約束**） |
| はたらき | `EFFECTS` / 座標 `EPOS` | 11 | data.js（手書き） |
| 関係型 | `REL` | 6 | data.js（手書き） |
| カテゴリ | `CATEMOJI` / `CATPOS` | 13 | data.js（手書き） |

### 中間表（＝つなぎ・粘菌の線の実体）

主役どうしの組み合わせ（鉄「と」豚もも 等）。単独では存在しない。グラフでは線（エッジ）になる。

| 名前 | つなぐもの | 件数 | 置き場所 | 線の意味 |
|---|---|---|---|---|
| `FOODS` | 栄養素 × 食材 | 602 | data.js（手書き） | **食材の遠近**（栄養素をどれだけ摂れるか）。点線判定もここ相当 |
| `PCT` | 栄養素 × 食材 | 602 | app-template.html（**csvから自動生成**） | 距離の実数値。`FOODS` と対になる |
| `E` | 栄養素 × 栄養素 | 26 | data.js（手書き） | 栄養素どうしの関係（吸収を助ける・邪魔し合う 等） |
| `EFF` | 栄養素 → はたらき | 51 | data.js（手書き） | はたらきの担い手（main / sub） |
| `BLOCK` | 栄養素 → はたらき | 4 | data.js（手書き） | 摂りすぎ注意の阻害線 |

### 付随テーブル（属性・表示）

| 名前 | 中身 | 置き場所 |
|---|---|---|
| `YOMI` | 食材の読み仮名（検索用） | data.js（手書き） |
| `NPOS` `EPOS` `CATPOS` | 栄養素・はたらき・カテゴリの配置座標 | data.js（手書き） |
| `CATLIST` | カテゴリ別の食材リスト | data.js（手書き） |
| `GRP` `BIG` `CORE` `WATER` `FATS` | 栄養素のフラグ（ヘム/非ヘム、水溶性/脂溶性 等） | data.js（手書き） |
| `REL` `EFFECTS` `SOURCES` `LEVELSRC` `DISCLAIMER` | ラベル・出典・注意書き | data.js（手書き） |

---

## パイプライン（何が何を生成するか）

```
amounts.csv ──[gen_pct.py]──> app-template.html 内の const PCT={...} を書き換え
                                        │
tabeawase-data.js ──────────────────────┤
（FOODS, CATLIST, YOMI 等を直書き）        │
                                        ▼
                              [build.py] で束ねる
                                        ▼
                                eiyou-zukan.html（配布物・手で触らない）
```

### ここが要注意

- **csv から自動生成されるのは `PCT` だけ。** `gen_pct.py` は app-template.html の `const PCT={...}` を1行書き換えるだけ。
- **`FOODS`・`CATLIST`・`YOMI` は自動生成されない。** data.js に手書きされていて、csv とは**運用ルールで揃える約束**になっているだけ（v0.6で手作業で一度揃えた）。
- つまり「FOODS は csv から生成が正」は**方針**であって、パイプラインには入っていない。**手で揃える。**
- `FOODS`(602) と `PCT`(602) と csv(602行) が今は一致している。ズレたら線と距離が食い違う。

---

## 波及表：食材を1つ足す／消すとき触る場所

| # | 触る場所 | ファイル | 手作業か | 内容 |
|---|---|---|---|---|
| 1 | `amounts.csv` | amounts.csv | 手 | 行を足す／消す（栄養素ごとに複数行） |
| 2 | `gen_pct.py` を実行 | — | コマンド | csv → `PCT` を再生成 |
| 3 | `FOODS` | tabeawase-data.js | 手 | `[栄養素id, 食材名, レベル]` を csv に合わせる |
| 4 | `CATLIST` | tabeawase-data.js | 手 | 該当カテゴリの食材リストに追加／削除 |
| 5 | `YOMI` | tabeawase-data.js | 手 | 読み仮名エントリを追加／削除 |
| 6 | `build.py` を実行 | — | コマンド | data.js＋template を束ねて配布HTMLを吐く |

**取りこぼしの典型**：csv だけ直して `FOODS`/`CATLIST`/`YOMI` を忘れる → 線が残る・検索に出る・カテゴリに残る、という幽霊が発生する。

### 栄養素・はたらき・カテゴリを足す／消すとき

- 栄養素 … `NUT`・`NPOS`・（必要なら）`E`・`EFF`・`BLOCK`・`GRP` 等のフラグ
- はたらき … `EFFECTS`・`EPOS`・`EFF`・`BLOCK`
- カテゴリ … `CATEMOJI`・`CATPOS`・`CATLIST`

これらは食材より変更頻度が低いが、座標（POS系）を忘れると配置が崩れる。

---

## 用語の対応（コード名 ⇄ 意味）

| コード | 意味 |
|---|---|
| `NUT` | 栄養素の定義（名前・単位） |
| `FOODS` | 栄養素×食材の紐付き（線の本体） |
| `PCT` | その線の距離（割合%）。csv生成 |
| `E` | 栄養素どうしの関係 |
| `REL` | 関係の型（excrete=排出を促す 等）の定義 |
| `EFF` | 栄養素→はたらき |
| `EFFECTS` | はたらきの定義（骨と歯をつくる 等） |
| `BLOCK` | 摂りすぎ注意の阻害 |
| `CATLIST` | カテゴリ別の食材一覧 |
| `YOMI` | 食材の読み仮名（検索用） |
| `NPOS`/`EPOS`/`CATPOS` | 栄養素／はたらき／カテゴリの配置座標 |
| エンティティ（主役） | 単独で存在するもの。栄養素・食材・はたらき・カテゴリ |
| ノード | 画面に点で描かれるもの。エンティティのうち栄養素・食材 |
| エッジ（線・つなぎ） | 主役どうしの組み合わせ。FOODS・E・EFF・BLOCK |

---

## この地図の使い方

- 何かを足す／消す前に「波及表」で触る場所を全部拾う。
- 作業後、`FOODS`件数・`PCT`件数・csv行数が一致しているか数える（今は 602 で揃い）。
- 将来ここを自動化するなら「レベル2＝整合チェックのスクリプト」「レベル3＝FOODS/CATLIST/YOMI も csv 生成に寄せる」を検討（引き継ぎメモの次にやること参照）。

---

## 出典

件数・構造は `tabeawase-data.js`・`amounts.csv`・`gen_pct.py`・`build.py`（v0.6時点）を実際に読んで確認した。
